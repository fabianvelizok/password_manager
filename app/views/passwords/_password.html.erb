<%= link_to password_path(password), class: "group block" do %>
  <div id="<%= dom_id(password) %>" class="rounded-xl border bg-white p-4 shadow-sm transition group-hover:bg-gray-50 group-hover:shadow-md">
    <h2 class="truncate text-base font-semibold text-gray-900">
      <%= password.url %>
    </h2>
  </div>
<% end %>